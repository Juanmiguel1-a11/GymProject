namespace Gym.Domain.Enums
{
    public enum MembershipStatus
    {
        Active=0,      // Activa
        Expired=1,     // Expirada
        Suspended=2,   // Suspendida
        Cancelled=3    // Cancelada
    }
}
